<Window x:Class="pizzeria.EditOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:pizzeria"
        mc:Ignorable="d"
        Title="Редагування" Style="{StaticResource WinStyleAddEdit}" Width="300" Height="200"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Оберіть новий статус:" Style="{StaticResource TextBlock}"/>

        <ComboBox x:Name="StatusComboBox" Grid.Row="1" Width="200" Height="25"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0" VerticalAlignment="Bottom">
            <Button Content="Зберегти"  Style="{StaticResource btn}" Click="SaveButton_Click"/>
            <Button Content="Відмінити" Style="{StaticResource btnOrange}" Width="80" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
