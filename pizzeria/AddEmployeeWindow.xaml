<Window x:Class="pizzeria.AddEmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Додати співробітника" Style="{StaticResource WinStyleAddEdit}"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Прізвище та ім'я:" Margin="0,0,0,5" FontSize="16" FontWeight="Bold" />
            <TextBox x:Name="FullNameTextBox" Width="200" Height="25" FontSize="14" Margin="0,0,0,10"/>

            <TextBlock Text="Посада:" Margin="0,0,0,5" FontSize="16" FontWeight="Bold"/>
            <ComboBox x:Name="PositionComboBox" Width="200" Height="25" FontSize="14" Margin="0,0,0,10"
                      DisplayMemberPath="position_name"
                      SelectedValuePath="position_id"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="Додати" Style="{StaticResource btn}" Click="AddButton_Click"/>
                <Button Content="Відмінити" Style="{StaticResource btnOrange}" Click="CancelButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
